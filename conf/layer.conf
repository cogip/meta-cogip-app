# meta-cogip-app layer configuration.

BBPATH .= ":${LAYERDIR}"

BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "cogip-app"
BBFILE_PATTERN_cogip-app = "^${LAYERDIR}/"
BBFILE_PRIORITY_cogip-app = "10"

LAYERSERIES_COMPAT_cogip-app = "scarthgap"

LAYERDEPENDS_cogip-app = " \
    core \
    openembedded-layer \
    raspberrypi \
    meta-python \
    multimedia-layer \
    filesystems-layer \
    webkit \
    virtualization-layer \
"

# Staging area for the (gitignored) pre-built container image tarball,
# produced by `make app-image`. Used by cogip-app-image.
COGIP_FILES_PREBUILT = "${LAYERDIR}/files-prebuilt"
