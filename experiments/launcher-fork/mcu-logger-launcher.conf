[Unit]
# Phase 5: the server now lives inside cogip-launcher, not cogip@server.
# Reset the inherited bind.conf (which targets cogip@server) and follow the
# launcher lifecycle instead.
After=
After=cogip-launcher.service
BindsTo=
BindsTo=cogip-launcher.service
