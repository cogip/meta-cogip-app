# meta-cogip-app

Cogip kiosk **application** layer (Yocto, Raspberry Pi 4): Cog/WPE
WebKit kiosk + the containerized cogip-tools stack + the SD image.
Active development on the `feat/yocto-cogip` branch.
