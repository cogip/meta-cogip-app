# Runtime socket directory shared between the cogip tool containers
# (compose maps /run/cogip -> /var/run/cogip). /run is tmpfs, so the
# directory is created at boot by systemd-tmpfiles, not shipped in the
# rootfs.
d /run/cogip 0755 root root -
