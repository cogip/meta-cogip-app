[Service]
# Clear stale shared-memory segments before the server creates new ones
# (host /dev/shm is shared with the container via ipc:host). Mirrors the
# raspios cogip-server ExecStartPre.
ExecStartPre=/bin/sh -c 'rm -f /dev/shm/cogip_* 2>/dev/null || true'
