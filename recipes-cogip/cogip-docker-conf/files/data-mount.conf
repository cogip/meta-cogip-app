[Unit]
# Docker stores everything under /data/docker (see daemon.json), so the
# daemon must not start until the data partition is mounted.
RequiresMountsFor=/data
After=data.mount
Requires=data.mount
